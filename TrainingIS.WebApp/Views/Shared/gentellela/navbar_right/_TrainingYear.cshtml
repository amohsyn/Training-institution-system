@using TrainingIS.Entities;
@using TrainingIS.Entities.Resources.TrainingYearResources;

<a href="javascript:;" class="user-profile dropdown-toggle hidden-xs" data-toggle="dropdown" aria-expanded="false">
    <span class=" fa fa-calendar"></span>
    <span >@msg_TrainingYear.SingularName</span> : @(ViewBag.CurrentTrainingYear?.Code)
    <span class=" fa fa-angle-down"></span>
</a>
 
<ul class="dropdown-menu dropdown-usermenu pull-right">

    @foreach (TrainingYear item in ViewBag.TrainingYears)
    {
        //if (ViewBag.CurrentTrainingYear != item)
        //{
    <li>
        
        <a href="@Url.Action("ChangeCurrentTrainingYear","TrainingYears", new { Code = item.Code , URL = Request.Url })">@item.Code</a>
    </li>
        //}
    }
</ul>

 








